NOT LEGAL RP — обновление страницы правил

Установка:
1. Скопируйте содержимое этого архива в корень проекта:
   C:\Users\serge\gta-wiki-clone
2. Подтвердите замену файлов.
3. Выполните:
   npm run build
   git add .
   git commit -m "Redesign rules page and rules manager"
   git push origin main

Что изменено:
- новая публичная страница /wiki/rules;
- новый интерфейс /admin/rules;
- общий поиск по всем разделам и содержимому;
- автоматическое меню по заголовкам H2/H3;
- версии разделов и дата последнего изменения;
- улучшенные ошибки загрузки и сохранения;
- сохранение по-прежнему работает через Redis на Vercel и JSON локально.
